# pinned for reproducibility — the upstream package wraps these directly
numpy==1.26.4
pandas==2.2.3
scipy==1.13.1
scikit-learn==1.5.2
tables==3.10.1                # PyTables, needed to read figshare .h5 files
# xgboost: standard linux pip wheel ships with CUDA support out of the box.
# 2.1.3 wheels are built against CUDA 12.x; an RTX 5060 Ti (Blackwell, sm_120)
# needs a recent driver/runtime but works with this wheel as of release.
# On macOS the wheel is CPU-only — that's fine, GPU is auto-detected at runtime.
xgboost==2.1.3
lifelines==0.30.0             # only needed if survival tasks are added later

# analysis + plotting
matplotlib==3.9.2
seaborn==0.13.2

# notebooks
jupyterlab==4.3.4
ipykernel==6.29.5

# downloads
requests==2.32.3
tqdm==4.67.1
